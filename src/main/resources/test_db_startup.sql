/* This script runs at the startup of the Application, though I have no idea how Spring figures this out.
   Perhaps it merely runs all SQL Scripts in the /resources directory ?
 */
/*
CREATE TABLE users (
    user_id int,
    user_first_name varchar (255),
    user_last_name varchar (255),

    PRIMARY KEY (user_id)
);
 */